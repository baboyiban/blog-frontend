services:
  frontend:
    platform: linux/amd64
    image: chl11wq12/blog-frontend:v0.1.0
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:80"
    restart: unless-stopped
